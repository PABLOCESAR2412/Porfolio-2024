---
import Layout from '../../layouts/Layout.astro';
import Navbar from '../../components/Navbar.astro'
import About from '../../components/About.astro'
import Project from '../../components/Project.astro'
import Skills from '../../components/Skills.astro'
import Experience from 'src/components/Experience.astro';
import Studies from 'src/components/Studies.astro';
import Courses from 'src/components/Courses.astro';
import Contact from 'src/components/Contact.astro';
import Footer from 'src/components/Footer.astro';
import en from '../../i18n/en.json';

const locale = 'en';
---

<Layout title="Porfolio Pablo Torres">
	<main class="z-10">
		<Navbar translations={en} locale={locale}/>
		<About translations={en} locale={locale}/>
		<Project translations={en} locale={locale}/>
		<Skills translations={en} locale={locale}/>
		<Experience translations={en} locale={locale}/>
		<Studies translations={en} locale={locale}/>
		<Courses translations={en} locale={locale}/>
		<Contact translations={en} locale={locale}/>
		<Footer translations={en} locale={locale}/>
	</main>
</Layout>
